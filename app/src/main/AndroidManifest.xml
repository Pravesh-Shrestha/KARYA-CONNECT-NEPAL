<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:replace="android:fullBackupContent"
        android:icon="@drawable/mainlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mainlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.KaryaConnectNepal"
        tools:targetApi="31">

        <activity
            android:name=".UI.Activity.DashboardActivityClient"
            android:exported="false">
        </activity>

        <activity
            android:name=".UI.Activity.DashboardActivityFreelancer"
            android:exported="false">
        </activity>

        <activity
            android:name=".UI.Activity.SplashPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.Activity.RegistrationActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".UI.Activity.LoginPage"
            android:exported="false">
        </activity>

        <activity
            android:name=".UI.Activity.MainActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>