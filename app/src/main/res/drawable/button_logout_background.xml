<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Default State (Red with Rounded Corners) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#ED2312"/> <!-- Red color -->
            <corners android:radius="30dp"/>
            <padding android:left="16dp" android:right="16dp" android:top="10dp" android:bottom="10dp"/>
        </shape>
    </item>

    <!-- Pressed State (Darker Red) -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#C21807"/>
            <corners android:radius="30dp"/>
        </shape>
    </item>

</selector>
